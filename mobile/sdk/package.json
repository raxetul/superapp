{
  "name": "@superapp/module-sdk-mobile",
  "private": true,
  "version": "1.0.0",
  "description": "SuperApp mobile module SDK (TR-09-003): the mobile module host interface (screens, components, permissions, initialize/cleanup) and types.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "peerDependencies": {
    "react": "*"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "@types/react": "~19.2.2",
    "react": "19.2.3",
    "typescript": "~6.0.3",
    "vitest": "^2.1.5"
  }
}
