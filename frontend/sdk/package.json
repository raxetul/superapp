{
  "name": "@superapp/module-sdk-web",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "SuperApp frontend module SDK (TR-09-002): the web module host interface (routes, components, permissions, initialize/cleanup) and types.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "peerDependencies": {
    "react": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "@types/react": "^18.3.12",
    "react": "^18.3.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  }
}
